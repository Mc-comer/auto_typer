# auto_typer
Mc-comer制作的打字机
使用方法:
1.打开requirements.txt，根据内容安装所需库
2.双击Python文件直接打开即可使用

使用方法2：
1.打开requirements.txt，根据内容安装所需库
2."pyinstaller --onefile --windowed --icon=icon.ico --version-file=version_info.txt --name=打字丸 auto_typer.py",打开cmd命令行工具直接通过这个命令打包成exe
3.双击点开打包后的exe文件,即可直接使用
